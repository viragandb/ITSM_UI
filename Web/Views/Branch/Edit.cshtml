@model Domain.Branch

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col-12">

        <div class="card">

            <div class="card-header d-flex justify-content-between align-items-center">

                <h5 class="card-title mb-0">
                    <i class="align-middle me-2" data-lucide="building-2"></i>
                    Location
                </h5>

                <a href="@Url.Action("Index", "Branch")"
                   class="btn btn-sm btn-light">

                    <i class="align-middle me-1" data-lucide="arrow-left"></i>
                    Back

                </a>

            </div>

            @using (Html.BeginForm("Edit", "Branch", FormMethod.Post))
            {
                @Html.AntiForgeryToken()
                @Html.ValidationSummary(true)

                @Html.HiddenFor(model => model.BranchId)

                <div class="card-body">

                    <div class="row g-3">

                        <div class="col-md-6">

                            @Html.LabelFor(
                                model => model.Name,
                                new { @class = "form-label" }
                            )

                            @Html.TextBoxFor(
                                model => model.Name,
                                new { @class = "form-control" }
                            )

                            @Html.ValidationMessageFor(
                                model => model.Name,
                                "",
                                new { @class = "text-danger" }
                            )

                        </div>

                        <div class="col-md-6">

                            @Html.LabelFor(
                                model => model.Email,
                                new { @class = "form-label" }
                            )

                            @Html.TextBoxFor(
                                model => model.Email,
                                new { @class = "form-control" }
                            )

                            @Html.ValidationMessageFor(
                                model => model.Email,
                                "",
                                new { @class = "text-danger" }
                            )

                        </div>

                        <div class="col-md-6">

                            @Html.LabelFor(
                                model => model.DAOCode,
                                new { @class = "form-label" }
                            )

                            @Html.TextBoxFor(
                                model => model.DAOCode,
                                new { @class = "form-control" }
                            )

                            @Html.ValidationMessageFor(
                                model => model.DAOCode,
                                "",
                                new { @class = "text-danger" }
                            )

                        </div>

                        <div class="col-md-6">

                            @Html.LabelFor(
                                model => model.EscalationEmail,
                                new { @class = "form-label" }
                            )

                            @Html.TextBoxFor(
                                model => model.EscalationEmail,
                                new { @class = "form-control" }
                            )

                            @Html.ValidationMessageFor(
                                model => model.EscalationEmail,
                                "",
                                new { @class = "text-danger" }
                            )

                        </div>

                        <div class="col-md-6">

                            @Html.LabelFor(
                                model => model.RegionId,
                                new { @class = "form-label" }
                            )

                            @Html.DropDownListFor(
                                model => model.RegionId,
                                new List<SelectListItem>(),
                                new { @class = "form-select" }
                            )

                            @Html.ValidationMessageFor(
                                model => model.RegionId,
                                "",
                                new { @class = "text-danger" }
                            )

                        </div>

                        <div class="col-md-6">

                            @Html.LabelFor(
                                model => model.DeliverySLAId,
                                new { @class = "form-label" }
                            )

                            @Html.DropDownListFor(
                                model => model.DeliverySLAId,
                                new List<SelectListItem>(),
                                new { @class = "form-select" }
                            )

                            @Html.ValidationMessageFor(
                                model => model.DeliverySLAId,
                                "",
                                new { @class = "text-danger" }
                            )

                        </div>

                    </div>

                </div>

                <div class="card-footer">

                    <div class="d-flex justify-content-end gap-2">

                        <button type="reset"
                                class="btn btn-danger btn-sm">

                            <i class="align-middle me-1" data-lucide="rotate-ccw"></i>
                            Cancel

                        </button>

                        <button type="submit"
                                class="btn btn-primary btn-sm">

                            <i class="align-middle me-1" data-lucide="save"></i>
                            Save

                        </button>

                    </div>

                </div>
            }

        </div>

    </div>
</div>

@section scripts {

    <script type="text/javascript">

        $(document).ready(function () {

            loadRegions();
            loadDeliverySLAs();

        });


        function parseJsonResult(data) {

            if (typeof data === "string") {

                try {
                    return JSON.parse(data);
                }
                catch (e) {
                    return [];
                }

            }

            return data || [];

        }


        function loadRegions() {

            $.getJSON(
                '@Url.Action("GetItemsJsonResult", "Region")',
                function (data) {

                    var dropDown =
                        $("#RegionId");

                    dropDown.empty();

                    dropDown.append(
                        "<option value='0'>-- Select --</option>"
                    );

                    $.each(
                        parseJsonResult(data),
                        function (index, item) {

                            dropDown.append(
                                $("<option/>", {
                                    value: item.RegionId,
                                    text: item.Name
                                })
                            );

                        }
                    );

                    var selectedId =
                        '@ViewBag.RegionId';

                    if (selectedId) {
                        dropDown.val(selectedId);
                    }

                }
            );

        }


        function loadDeliverySLAs() {

            $.getJSON(
                '@Url.Action("GetItemListJsonResult", "DeliverySLA")',
                function (data) {

                    var dropDown =
                        $("#DeliverySLAId");

                    dropDown.empty();

                    dropDown.append(
                        "<option value='0'>-- Select --</option>"
                    );

                    $.each(
                        parseJsonResult(data),
                        function (index, item) {

                            dropDown.append(
                                $("<option/>", {
                                    value: item.DeliverySLAId,
                                    text: item.Name
                                })
                            );

                        }
                    );

                    var selectedId =
                        '@ViewBag.DeliverySLAId';

                    if (selectedId) {
                        dropDown.val(selectedId);
                    }

                }
            );

        }

    </script>
}